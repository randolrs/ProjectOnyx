<h4>Top Articles</h4>

	<ol type="1" style="font-size: 16px;">
		
		<% @top_articles.each do |article| %>
			<a href="<%=predictorarticleshow_path(Predictor.find(article.predictor_id).username, article.id)%>">
				<li class="top-article-item"><%= article.title %></li>
			</a>

		<%end%>

	</ol>
