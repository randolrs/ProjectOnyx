
<% if user_signed_in?%>
	<%unless Purchase.exists?(:user_id=> current_user.id,:predictor_id=>@predictor.id) %>

  		<%= link_to "+ Follow", follow_predictor_path(:username=>@predictor.username), {:class=>"follow_cta" }%>

	<% end %>
<%end%>
