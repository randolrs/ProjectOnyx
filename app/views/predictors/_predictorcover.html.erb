<div class="predictor-cover">
  
  <div class="predictor-cover-container"> 

    <h1><%= image_tag @predictor.image.url(:medium, :width => 50, :height => 50), class: "predictor-image-small"%><%= link_to @predictor.username, predictordashboard_path(@predictor.username), class: "predictor-name"%></h1><%= render partial: 'predictors/follow_cta', locals: {predictor: @predictor} %>

    <br>
    
    <a><%=@predictor.bio%></a>

    <%# bar_chart Predictor.group(:balance).sum(:balance) %>

  </div>

</div>