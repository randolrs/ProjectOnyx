<div class = "user-dashboard-prediction-nav">

	<ul class="dashboard-prediction-nav">
		
		<%unless @subaction == "games" or @subaction == "top" %>
			<li class="active"><%= link_to "Recent Predictions", teamshow_path(@league,@team.id) %></li>
		<%else%>
			<li><%= link_to "Recent Predictions", teamshow_path(@league,@team.id) %></li>
		<%end%>

		<%if @subaction == "games"%>
			<li class="active"><%= link_to "All Games", teamshowgames_path(@league, @team.id) %></li>
		<%else%>
			<li><%= link_to "All Games", teamshowgames_path(@league, @team.id) %></li>
		<%end%>

		<%if @subaction == "top"%>
			<li class="active"><%= link_to "Top Experts", teamshowpredictors_path(@league, @team.id) %></li> 
		<%else%>
			<li><%= link_to "Top Experts", teamshowpredictors_path(@league, @team.id) %></li>
		<%end%>

	</ul>

</div>