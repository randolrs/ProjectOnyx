<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @team.name %>

  <br>

  <strong>Id:</strong>
  <%= @team.id %>

  <br>

  <strong>League:</strong>
  <%= @team.league %>

  <strong>Team Image:</strong>
  <%= image_tag @team.image.url(:medium) %>
</p>


Games:

<table>
  <thead>
    <tr>
      <th>Date</th>
      <th>Home Team</th>
      <th>Away Team</th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>

      <% if game.teama_id == @team.id or game.teamh_id == @team.id %>
        <tr>
          <td><%= game.event_time.strftime("%D")  %></td>
          <td><%= game.teamh %></td>
          <td><%= game.teama %></td>
        </tr>

      <% end %>
    <% end %>
  </tbody>
</table>

<%= link_to 'Edit', edit_team_path(@team) %> |
<%= link_to 'Back', teams_path %>
